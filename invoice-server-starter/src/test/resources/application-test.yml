spring:
  main:
    banner-mode: off
  jpa:
    show-sql: false
  flyway:
    locations:
      - classpath:db/migration
      - classpath:db/test-data

logging:
  level:
    root: WARN
    org.testcontainers: WARN
    io.restassured: WARN
